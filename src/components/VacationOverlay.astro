---
---

<div id="vacation-overlay" class="fade-in overflow-y-auto">
  <div class="app-wrapper p-6">
    <div class="flex justify-between items-center mb-8">
      <div>
        <h2 class="text-3xl font-bold tracking-tight">Maximizador de vacaciones <span class="text-green">PRO</span></h2>
        <p class="text-gray-500 dark:text-gray-400">Optimiza tus días para descansar más.</p>
      </div>
      <button id="btn-close-vacation" class="text-red font-bold text-lg">Cerrar</button>
    </div>

    <div class="bg-gray-50 dark:bg-zinc-900 p-6 rounded-3xl mb-10 border border-gray-100 dark:border-zinc-800">
      <label class="block text-sm font-bold text-gray-400 uppercase tracking-widest mb-3 text-balance">
        Ingresa el número de días de vacaciones acumuladas
      </label>
      <div class="flex gap-4 mb-6">
        <input type="number" id="vacation-days-input" value="15" min="1" max="999" maxlength="3" class="ios-input text-2xl font-bold w-32" oninput="if(this.value.length > 3) this.value = this.value.slice(0,3);">
        <div class="flex-1 text-sm text-gray-500 leading-tight flex items-center">
          Calculamos las mejores opciones o elige tú mismo el inicio.
        </div>
      </div>
      <button id="btn-manual-mode" class="w-full py-4 bg-green-500 text-white font-bold rounded-2xl active:scale-95 transition-transform flex items-center justify-center gap-2">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 15l-2 5L9 9l11 4-5 2zm0 0l5 5M7.188 2.239l.777 2.897M5.136 7.965l-2.898-.777M13.95 4.05l-2.122 2.122m-5.657 5.656l-2.12 2.122"></path>
        </svg>
        Seleccionar manualmente en el calendario
      </button>
    </div>
    
    <h3 id="vacation-results-title" class="text-xs font-bold text-gray-400 uppercase tracking-widest mb-6 px-2">
      Top 3 Mejores Opciones
    </h3>
    <div id="vacation-results" class="space-y-6 pb-20"></div>
  </div>
</div>
